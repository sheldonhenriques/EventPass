<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Events</title>
</head>
<body>
  <h1>Events</h1>
  <ul>
    <% events.forEach(event => { %>
      <li>
        <h2><%= event.name %></h2>
        <p><%= event.summary %></p>
      </li>
    <% }); %>
  </ul>
</body>
</html>
