<% events.forEach(event => { %>
    <% if (event.event) { %>
      <%- include('event-card', { event: event.event }) -%>
    <% } else { %>
      <%- include('event-card', { event: event }) -%>
    <% } %>
  <% }) %>